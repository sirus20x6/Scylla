# Scylla Plugins
cmake_minimum_required(VERSION 3.15)

message(STATUS "Scylla plugins support - to be implemented")

# Plugin architecture will be added in future updates
# This is a placeholder to ensure the build system is complete

# Plugins would typically be built as shared libraries that
# can be loaded dynamically by Scylla at runtime
